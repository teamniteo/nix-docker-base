name: Update
on:
  schedule:
    - cron: '0 * * * *'
  push:
    branches:
      - master
      - pinned-nixpkgs

jobs:
  niv-update:
    name: Update
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: cachix/install-nix-action@v10
    - name: Run updater
      run: |
        nix-build -A updater
        result/bin/update
